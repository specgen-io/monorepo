<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        {{#server.spring}}
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>{{versions.spring_boot.value}}</version>
        <relativePath/> <!-- lookup parent from repository -->
        {{/server.spring}}
        {{#server.micronaut}}
        <groupId>io.micronaut</groupId>
        <artifactId>micronaut-parent</artifactId>
        <version>{{versions.micronaut.value}}</version>
        {{/server.micronaut}}
    </parent>

    <groupId>{{groupid.value}}</groupId>
    <artifactId>{{project.value}}</artifactId>
    <packaging>jar</packaging>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <specgen.version>{{versions.specgen.value}}</specgen.version>
        {{#server.spring}}
        <java.version>11</java.version>
        <spring.version>{{versions.spring_boot.value}}</spring.version>
        <springfox.version>{{versions.springfox.value}}</springfox.version>
        {{/server.spring}}
        {{#server.micronaut}}
        <jdk.version>11</jdk.version>
        <release.version>11</release.version>
        <micronaut.version>{{versions.micronaut.value}}</micronaut.version>
        <micronaut.runtime>netty</micronaut.runtime>
        <exec.mainClass>{{package.value}}.{{mainclass.value}}</exec.mainClass>
        {{/server.micronaut}}
        {{#jsonlib.moshi}}
        <moshi.version>{{versions.moshi.value}}</moshi.version>
        <jsr305.version>{{versions.jsr305.value}}</jsr305.version>
        {{/jsonlib.moshi}}
    </properties>

    <dependencies>
        {{#server.spring}}
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        {{#swagger.value}}
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger2</artifactId>
            <version>${springfox.version}</version>
        </dependency>
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-boot-starter</artifactId>
            <version>${springfox.version}</version>
        </dependency>
        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-swagger-ui</artifactId>
            <version>${springfox.version}</version>
        </dependency>
        {{/swagger.value}}
        {{/server.spring}}
        {{#server.micronaut}}
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-inject</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-validation</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-http-server-netty</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-runtime</artifactId>
            <scope>compile</scope>
        </dependency>
        {{#jsonlib.jackson}}
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-jackson-databind</artifactId>
            <scope>compile</scope>
        </dependency>
        {{/jsonlib.jackson}}
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>runtime</scope>
        </dependency>
        {{#swagger.value}}
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations</artifactId>
            <scope>compile</scope>
        </dependency>
        {{/swagger.value}}
        {{/server.micronaut}}
        {{#jsonlib.moshi}}
        <dependency>
            <groupId>com.squareup.moshi</groupId>
            <artifactId>moshi</artifactId>
            <version>${moshi.version}</version>
        </dependency>
        <dependency>
            <groupId>com.squareup.moshi</groupId>
            <artifactId>moshi-adapters</artifactId>
            <version>${moshi.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
            <version>${jsr305.version}</version>
          </dependency>
        {{/jsonlib.moshi}}
    </dependencies>

    <build>
        <plugins>
            {{#server.spring}}
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.version}</version>
            </plugin>
            {{/server.spring}}
            {{#server.micronaut}}
            <plugin>
                <groupId>io.micronaut.build</groupId>
                <artifactId>micronaut-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths combine.children="append">
                        <path>
                            <groupId>io.micronaut</groupId>
                            <artifactId>micronaut-http-validation</artifactId>
                            <version>${micronaut.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-Amicronaut.processing.group={{groupid.value}}</arg>
                        <arg>-Amicronaut.processing.module={{project.value}}</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            {{/server.micronaut}}
            <plugin>
                <groupId>io.specgen</groupId>
                <artifactId>java-maven-plugin</artifactId>
                <version>${specgen.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>service-java</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <jsonlib>{{jsonlib.value}}</jsonlib>
                    <specFile>${project.basedir}/spec.yaml</specFile>
                    <packageName>{{package.value}}</packageName>
                    <server>{{server.value}}</server>
                    {{#swagger.value}}
                    <swaggerPath>src/main/resources/static/docs/swagger.yaml</swaggerPath>
                    {{/swagger.value}}
                    <servicesPath>src/main/java</servicesPath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
